.container-fluid
  .row
    .col-md-12
      - flash.each do |key, value|
        %div{ role: "alert", class: "alert alert-dismissible alert-#{key == 'notice' ? 'success' : 'danger' }" }
          %button.close{"aria-label" => "Close", "data-dismiss" => "alert", :type => "button"}
            %span{"aria-hidden" => "true"} ×
          = value
