.row
  .col-md-4.col-md-offset-4
    .panel.panel-default
      .panel-body
        = simple_form_for shallow_args(@list, @todo_item) do |f|
          = f.error_notification

          .form-inputs
            = f.input :name
            = f.input :done

          .form-actions
            = f.button :submit
